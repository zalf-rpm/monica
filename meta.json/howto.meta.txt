** parameter meta description **

{
  /* required */
  
    desc: not null, string: description

  /* not required if enum or db is present */   
    unit: not null, string: "-", "bool", "date" or any other physical unit. 
  
  /* optional */
  
    default: null, string or number
    min: null, string or number,
    max: null, string or number,
    advanced: not null, true or false  (for "advanced" users or probably not implemented or experimental)
    enum: not null, array with strings or numbers e.g. ["option1", "option2"]
    OR
    db: not null, object { table: "tableName", columns: ["columnName1", "columnName2", ..], keys: ["columnName1", ...] }
}  


** units / examples **

Boolean
{
  unit: "bool",
  min: false,
  max: true,
  default: true or false
}  

Date (default date format is "YYYY-MM-DD")
{
  unit: "date",
  min: any date as "YYYY-MM-DD",
  max: any date as "YYYY-MM-DD",
  default: any date within min max
}

Percent, usually implies min = 0.0 and max = 100.0
{
  unit: "%" or e.g. "100 kg kg-1",
  ...
}

Physical fraction, decimal, usually implies min = 0.0 and max = 1.0
{ 
  unit: "kg (DM) kg-1 (DM)"
  ...
}

No unit
{
  unit: "-"
  ...
}

